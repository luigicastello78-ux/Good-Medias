{
  "name": "good-medias",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "eleventy --serve --port=5510",
    "build": "eleventy",
    "clean": "node -e \"import('fs').then(fs=>fs.rmSync('_site',{recursive:true,force:true}))\""
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "luxon": "^3.5.0"
  }
}
